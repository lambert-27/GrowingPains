java -jar GrowingPains.jar;

mysqldump -u root -p GrowingPains > GrowingPains.sql;

mysql -u root -p GrowingPains < GrowingPains.sql;

ALTER TABLE product RENAME TO Product;

ALTER TABLE customer RENAME TO Customer;

ALTER TABLE orders RENAME TO Orders;